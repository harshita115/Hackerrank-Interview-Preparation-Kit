#include <bits/stdc++.h>

using namespace std;

int main()
{
    int n, i, arr[100], h[100]={0}, count=0;
    cin>>n;
    for(i=0;i<n;i++)
    {
        cin>>arr[i];
        h[arr[i]]++;
    }
    for(i=0;i<=100;i++)
    {
         count += h[i]/2;
    }
    cout<<count;
    return 0;
}
